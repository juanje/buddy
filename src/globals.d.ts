// Build-time constants injected by Vite (see vite.config.ts `define`).

/** Absolute path of the project root at build time (dev-mode worker spawn). */
declare const __BUDDY_PROJECT_ROOT__: string;

/** App semver from package.json at build time. */
declare const __APP_VERSION__: string;
