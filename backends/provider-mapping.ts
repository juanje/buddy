// backends/provider-mapping.ts — Re-export shared provider mapping (FR-SETUP-05).

export { DEFAULT_SETUP_PROVIDER, supportsOAuth } from "../shared/provider-constants";
export { fromPiProviderId, toPiProviderId, WIZARD_PI_PROVIDERS } from "../shared/provider-mapping";
