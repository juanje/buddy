node_modules/
dist/
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/binaries/
.worker-build/
*.log
.DS_Store
fixtures/consolidation-test/

# Eval question banks contain personal data from specific Buddy instances
eval-questions.json

# Editor and OS leftovers. Never content, and the embedded-asset snapshot
# walks the working tree, so a stray swap file would end up in the sidecar.
*.sw[a-z]
*~
