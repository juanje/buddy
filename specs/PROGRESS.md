# Progress

Machine-tracked in `specs/progress.json`. Use the CLI:

```bash
npx tsx scripts/progress.ts current
npx tsx scripts/progress.ts status
```

For the full development methodology, see `docs/METHODOLOGY.md`.
